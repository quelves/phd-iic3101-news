<div class="container">
  <div class="alert alert-info text-center" role="alert">
    Ultimas Noticias
  </div>
  <div class="row">
      <% @news[0..9].each do |news| %>
      <div class="col-md-4 img_new_home">
          <article>
            <div class="pic_news_section"> <a href="/u2-joshua-tree-bono-or-without-you-565383">
              <!--picture class="" width="391" height="261">
                <img width="391" height="261" class="img-responsive" src="http://lorempixel.com/640/480/sports/" alt="U2" title="">
              </picture -->
            </div>
            <div class="">
              <!--div class=""> <a href="/Deportes">Deportes</a -->
              </div><h5><%= link_to news.title, news %></h5>
              <div class="summary">
                <% if news.subtitle.length > 249 %>
                  <%= news.subtitle[0..249] + "..." %>
                <% else %>
                    <%= news.subtitle%>
                <% end %>
                <% if news.comments.count > 0 %>
                    <a href="#" title="<%= news.comments.count %> Comentarios"> <span class="badge"><%= news.comments.count %></span></a><br>
                <% end %>
              </div>
            </div>
          </article>


      <% end %>

  </div>
</div>
